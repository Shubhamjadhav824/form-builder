# form-builder
Build a simplified Google Forms–like system where an admin can create dynamic forms and users can submit responses. The system must store form structures dynamically in the database and provide an admin dashboard to view submissions and basic analytics.
